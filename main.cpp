#include <iostream>
#include "CLI.h"

int main() {
    CLI cli;
    cli.start();

    // Thats an example of adding trees
    // Tree tree("+ 2 3");
    // Tree t2(tree);
    // Tree t3("- 4 5");
    // t2 = tree + t3;
    // std::cout << tree.to_string() << std::endl;
    // std::cout << t2.to_string() << std::endl;
    // std::cout << t3.to_string() << std::endl;

    return 0;
}